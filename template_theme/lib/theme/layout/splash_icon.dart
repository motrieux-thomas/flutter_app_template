import 'package:flutter/material.dart';

class AppSplashIcon extends StatelessWidget {
  const AppSplashIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: FlutterLogo(),
    );
  }
}
